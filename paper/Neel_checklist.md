# neel plan

**claims**
1) we


we train saes on toy model w relmag comp
we find they reliably learn latents with graded activations: can scale to swap output
==> sae latents not independently interpretable
this means several things:
- cannot treat sae latents as binary on/off switches for feature presence: magntude affects what feature is represented
- sae latents not independently interpretable: special latent seems to represent 


**key results** - saes learn graded latent activations in this toy model