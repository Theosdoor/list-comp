\section{Further Latent Steering Examples}\label{app:further_steering}

% NOTE
% 1) negative o1
% 2) no o2 in bounds
% 3) never predicts 2x & no o2 xover (looks same to never prdeict d1 at o2)
% 4) d1=d2

\begin{figure*}[htbp]
    \centering
    \includegraphics[width=0.95\linewidth]{figures/steering_feat11_o1_negative_scale.pdf}
    \caption{
        For 9.2\% of inputs, latent 11 (in the BatchTopK SAE from Section \ref{s:method}) must be negatively scaled to swap the predicted symbol at one of the output positions.
    }
    \label{fig:steering_feat11_o1_negative_scale}
\end{figure*} 


Figure \ref{fig:steering_feat11_o1_negative_scale} shows an example where swapping $o_1$ requires negative scaling ($p < 0$). The logit for $d_2$ at $o_1$ only overtakes $d_1$ when the latent activation is suppressed below zero, which is outside the valid activation range of a BatchTopK SAE.

\begin{figure*}[htbp]
    \centering
    \includegraphics[width=0.95\linewidth]{figures/steering_feat11_no_o2_crossover_in_bounds.pdf}
    \caption{
        For 2.3\% of inputs, it is possible to scale latent 11 (in the BatchTopK SAE from Section \ref{s:method}) to swap the symbol at each output position individually but not simultaneously.
    }
    \label{fig:steering_feat11_no_o2_crossover_in_bounds}
\end{figure*} 

Figure \ref{fig:steering_feat11_no_o2_crossover_in_bounds} shows an example where the swap ranges for $o_1$ and $o_2$ are mutually exclusive: there exists a positive $p$ that swaps each position individually, but no single $p$ swaps both simultaneously.

\begin{figure*}[htbp]
    \centering
    \includegraphics[width=0.95\linewidth]{figures/steering_feat11_o1_never_predicts_d2.pdf}
    \caption{
        For 1.6\% of inputs, the logit for the symbol at $d_2$ is always dominant at $o_2$ in the observed latent scale range (in the BatchTopK SAE from Section \ref{s:method}), so there is no scale where the symbol at $d_1$ is predicted instead.
    }
    \label{fig:steering_feat11_o1_never_predicts_d2}
\end{figure*} 

\begin{figure*}[htbp]
    \centering
    \includegraphics[width=0.95\linewidth]{figures/steering_feat11_o2_never_predicts_d1.pdf}
    \caption{
        For 0.2\% of inputs, the logit for the symbol at $d_1$ is always dominant at $o_1$ in the observed latent scale range (in the BatchTopK SAE from Section \ref{s:method}), so there is no scale where the symbol at $d_2$ is predicted instead.
    }
    \label{fig:steering_feat11_o2_never_predicts_d1}
\end{figure*} 

Figures \ref{fig:steering_feat11_o1_never_predicts_d2} and \ref{fig:steering_feat11_o2_never_predicts_d1} show examples where no swap occurs across the full observed range $p \in [0, 10]$. In Figure \ref{fig:steering_feat11_o1_never_predicts_d2}, the logit for $d_1$ remains dominant at $o_1$ for all $p$; Figure \ref{fig:steering_feat11_o2_never_predicts_d1} shows the analogous failure at $o_2$.


\begin{figure*}[htbp]
    \centering
    \begin{minipage}[t]{0.3\linewidth}
        \centering
        \includegraphics[width=\linewidth]{figures/41_49_a.pdf}
        \caption{
            In the BatchTopK SAE from Section \ref{s:method}, latent 11 cannot be scaled to swap input bigram $(41,49)$ because the logit for $49$ is never argmax in position $o_1$, as shown by the top plot.
        }
        \label{fig:41_49_a}
    \end{minipage}
    \hfill
    \begin{minipage}[t]{0.3\linewidth}
        \centering
        \includegraphics[width=\linewidth]{figures/41_49_b.pdf}
        \caption{
            However, in the same SAE, latent 55 co-activates with latent 11 for input $(41,49)$ and can be scaled by factor $p \in [1.07, 1.18]$ to cause the model to output $(49,41)$ instead.
        }
        \label{fig:41_49_b}
    \end{minipage}
    \hfill
    \begin{minipage}[t]{0.3\linewidth}
        \centering
        \includegraphics[width=\linewidth]{figures/41_49_c.pdf}
        \caption{
            If latent 55's activation magnitude is first scaled by a factor of 1.1, then latent 11 can be scaled by factor $p \in [0.03, 1.45]$ to swap the outputs. This was not possible without scaling latent 55. This suggests that some graded activation demonstrations are only possible when multiple active latents are scaled simultaneously.
        }
        \label{fig:41_49_c}
    \end{minipage}
\end{figure*}

In limited experiments with inputs that fail for the first three causes, it is sometimes possible to scale a co-activating non-special latent and successfully swap outputs. 
Figures \ref{fig:41_49_a}, \ref{fig:41_49_b} and \ref{fig:41_49_c} show one example. Input $(41,49)$ activates latents 11 (magnitude 3.1), 0 (magnitude 3.1) and 55 (magnitude 2.5). The swap fails with latent 11 because there are no positive overlapping scale ranges. However, scaling latent 55 by $p \in [1.1, 1.15]$ successfully produces a swap. This does not necessarily indicate that latent 55 is graded. Instead, scaling it alters the logit distribution, which opens a valid swap zone for latent 11. Similarly, running the steering pipeline on latent 55 alone swaps only 4 inputs, all of which co-activate latent 11. This suggests a similar mechanism. This preliminary experiment is insufficient for statistical significance, but it suggests that scaling multiple latents together may enable more output swaps than steering special latents alone.

\begin{table}[ht]
\centering
\caption{We perform steering experiments on several chosen and randomly sampled SAE latents to demonstrate how steering with special latents (\textbf{bold}) compares to non-special latents. We record how many outputs are successfully swapped by steering only that latent, how many do not activate the latent, and how many activate the latent but cannot be swapped. We additionally report the proportion of the activating inputs that are swapped (Cond. Swap).}
\label{tab:latent_swap_metrics}
\vspace{0.5em}
\begin{tabular}{@{}ccccc@{}}
\toprule
\textbf{Latent} & \textbf{Swapped} & \textbf{No Activation} & \textbf{Active \& No Swap} & \textbf{Cond. Swap} \\
\midrule
0   & 6 (0.1\%)  & 9786 (97.9\%) & 208 (2.1\%)  & 2.8\% \\
5   & 23 (0.2\%)  & 9594 (95.9\%) & 383 (3.8\%)  & 5.7\% \\
\textbf{11}  & \textbf{4976 (49.8\%)} & \textbf{3600 (36.0\%)} & \textbf{1424 (14.2\%)} & \textbf{77.8\%} \\
55  & 4 (0.0\%)  & 9787 (97.9\%) & 209 (2.1\%)  & 1.9\% \\
\textbf{56}  & \textbf{196 (2.0\%)}  & \textbf{9009 (90.1\%)} & \textbf{795 (7.9\%)}  & \textbf{19.8\%} \\
63  & 4 (0.0\%)  & 9801 (98.0\%) & 195 (2.0\%)  & 2.0\% \\
82  & 4 (0.0\%)  & 9787 (97.9\%) & 209 (2.1\%)  & 1.9\% \\
96  & 0 (0.0\%)  & 9974 (99.7\%) & 26 (0.3\%)  & 0.0\% \\
117 & 37 (0.4\%)  & 9782 (97.8\%) & 181 (1.8\%)  & 17.0\% \\
\bottomrule
\end{tabular}
\end{table}

We do not attempt steering on all latents due to computational constraints. Instead, we test five randomly selected latents (0, 55, 63, 82, 117), latent 96 (the non-special latent with highest $\Delta \alpha$ correlation, $r=0.0036$), and latent 5 (shown in Figure \ref{fig:sae-heatmap}). Results are shown in Table \ref{tab:latent_swap_metrics}. 
Every successful swap using a non-special latent has a co-activating special latent. This suggests that these non-special latents are unlikely to be graded. Instead, they may alter the logit distribution so that graded special latents can successfully swap outputs.
